import {User} from './user';

export class GameMode {
  id: number;
  name : string;
  death_dice: number;
  max_score: number;
  dice_count: number;
  max_dice_role: number;
  date: string;
  creator: number;
}
